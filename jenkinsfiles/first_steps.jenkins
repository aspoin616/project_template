#!groovy
//check properties
properties({disableConcurrentBuilds()})

pipeline {
    agent{
       label 'master'
       }
     options {
        buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
        timestamps()
        }
}
stages {
  stage("Preparations") {
    steps {
    sh 'ssh root@192.168.56.107 \'hostname\''
   }
  }
 }
}
